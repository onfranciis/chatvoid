const Input = () => {
  return (
    <div className="Input">
      <button>Receive</button>
      <textarea name="" id="" placeholder="Type a message..."></textarea>
      <button>Send</button>
    </div>
  );
};

export default Input;
